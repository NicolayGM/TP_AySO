ssh-keygen -t rsa -b 4096
cat ~/.ssh/id_rsa.pub
ssh -T git@github.com
cd ~
mkdir -p repogit
cd repogit
git clone git@github.com:NicolayGM/TP_AySO.git
cd TP_AySO
cat /etc/passwd
man grep
grep UBUNTU /etc/passwd 
grep ubuntu /etc/passwd
grep -i Ubuntu /etc/passwd
grep -i ubuntu /etc/passwd | awk -F: '{print $2}' > datos_usuario.txt
whoami
echo "Usuario=$(whoami)" >> datos_usuario.txt
cat datos_usuario.txt
git config --global user.email "nicolasschen@hotmail.com "
git config --global user.name "NicolayGM"
git add .
git commit -m "ADD: agregado 1er ejercicio sobre datos_usuarios.txt"
git status
grep "model name" /proc/cpuinfo | head -n1 > info_pc.txt
git add info_pc.txt
git commit -m "feat: Añadiendo información de CPU"
grep -v nologin /etc/passwd | head -10 | sort -t: -k7 > lista_ordenada
git add lista_ordenada
git commit -m "ADD: Listado de usuarios ordenada"
cat >> README.md << EOF
Alumno: Nicolas Schenfeld División: 115 Turno: Mañana
EOF

git add README.md
git commit -m "docs: Agregada información del alumno"
mkdir -p ~/TP1/{arquitectura/Clase{1..5},so/Clase{1..6}}
tree ~/TP1
ls -R ~/TP1
echo "mkdir -p carpeta1/carpeta2/carpeta3" > script_dir.sh
echo "mkdir -p ~/TP1/{arquitectura/Clase{1..5},so/Clase{1..6}}" > script_dir.sh
chmod +x script_dir.sh
git add script_dir.sh
git commit -m "ADD: Añadiendo script"
git push
history -a
$ cp ~/.bash_history "Historial_comandos_TuSchenfeldNicolas.txt"~
$ cp ~/.bash_history "Historial_comandos_TuSchenfeldNicolas.txt"
